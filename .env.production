VITE_API_URL=https://canopy-admin-418538192879.us-central1.run.app
# Static 3D/image assets (models, accessories, agent images) — served from
# object storage, not canopy-admin's own Cloud Run container. The full set is
# 3GB+, well beyond what's practical to ship in that container or commit to
# git. See GitHub issue #19 and #20 for the bucket setup this points at.
VITE_ASSET_BASE_URL=https://storage.googleapis.com/canopy-admin-server-assets
